pipeline {
    agent any

    stages {

        stage('Build') {
            steps {
                echo 'Build exitoso (simulado)'
            }
        }

        stage('Docker Build') {
            steps {
                echo 'Docker build simulado'
            }
        }

        stage('Test') {
            steps {
                echo 'Tests ejecutados'
            }
        }

        stage('Deploy') {
            steps {
                echo 'Deploy simulado'
            }
        }
    }
}